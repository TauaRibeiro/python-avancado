# Crie uma classe chamada ListaInvertida que encapsula uma lista. Implemente os seguintes métodos dunder:

# __init__(self, lista): Inicializa a classe com uma lista.
# __repr__(self): Retorna uma representação da lista invertida no formato ListaInvertida([elementos]).
# __getitem__(self, index): Permite acessar os elementos da lista de forma invertida (por exemplo, lista_invertida[0] deve retornar o último elemento da lista original).
# __len__(self): Retorna o comprimento da lista.